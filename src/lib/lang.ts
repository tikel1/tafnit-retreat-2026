/**
 * Hebrew-only site. We keep this module for parity with tuscany's
 * codebase shape, but the type collapses to a single value.
 */
export type Lang = "he";

export const DEFAULT_LANG: Lang = "he";
